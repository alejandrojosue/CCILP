'use strict';

/**
 * sector-desempeno controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sector-desempeno.sector-desempeno');
