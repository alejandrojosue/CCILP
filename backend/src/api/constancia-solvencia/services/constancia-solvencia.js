'use strict';

/**
 * constancia-solvencia service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::constancia-solvencia.constancia-solvencia');
