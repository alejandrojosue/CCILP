'use strict';

/**
 * constancia-solvencia router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::constancia-solvencia.constancia-solvencia');
