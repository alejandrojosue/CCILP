'use strict';

/**
 * constancia-solvencia controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::constancia-solvencia.constancia-solvencia');
